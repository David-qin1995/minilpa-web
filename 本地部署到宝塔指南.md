# 本地开发 → GitHub → 宝塔服务器 完整流程

## 📋 工作流程

```
本地电脑开发
    ↓
git push 到 GitHub
    ↓
GitHub Actions 触发
    ↓
自动部署到宝塔服务器
    ↓
网站自动更新 ✨
```

---

## 🚀 一次性设置（只需要做一次）

### 第一步：从服务器克隆代码到本地

```bash
# 在本地电脑执行

# 克隆仓库（替换成您的仓库地址）
git clone https://github.com/YOUR_USERNAME/minilpa-web.git

# 进入项目目录
cd minilpa-web

# 安装依赖
npm install

# 测试本地运行
npm run dev
# 访问：http://localhost:3000
```

---

### 第二步：配置 Git 凭证（避免每次输入密码）

#### 方案 A：使用 SSH（推荐）

```bash
# 1. 生成 SSH 密钥（如果还没有）
ssh-keygen -t ed25519 -C "your.email@example.com"
# 一路回车

# 2. 查看公钥
cat ~/.ssh/id_ed25519.pub
# Windows: type %USERPROFILE%\.ssh\id_ed25519.pub

# 3. 复制公钥，添加到 GitHub
# GitHub → Settings → SSH and GPG keys → New SSH key → 粘贴

# 4. 测试连接
ssh -T git@github.com
# 看到 "Hi username!" 就成功了

# 5. 修改远程仓库地址为 SSH
git remote set-url origin git@github.com:YOUR_USERNAME/minilpa-web.git
```

#### 方案 B：使用 Token + Git Credential

```bash
# 配置 Git 记住凭证
git config --global credential.helper store

# 下次 push 输入一次 Token，之后就会记住
```

---

## 💻 日常开发流程

### 1. 开发功能

```bash
# 拉取最新代码
git pull

# 创建功能分支（可选）
git checkout -b feature/new-feature

# 修改代码
# 使用你喜欢的编辑器...

# 本地测试
npm run dev
```

### 2. 提交代码

```bash
# 查看修改
git status

# 添加修改的文件
git add .

# 提交
git commit -m "feat: 添加新功能描述"

# 推送到 GitHub（触发自动部署）
git push origin main
# 如果是分支：git push origin feature/new-feature
```

### 3. 自动部署

- 代码推送后，GitHub Actions 自动触发
- 打开 GitHub 仓库 → Actions 查看进度
- 等待 8-10 分钟，部署完成
- 访问 http://esim.haoyiseo.com 查看更新

---

## 📝 Commit 消息规范（推荐）

```bash
# 新功能
git commit -m "feat: 添加用户登录功能"

# 修复 Bug
git commit -m "fix: 修复配置文件删除失败的问题"

# 文档更新
git commit -m "docs: 更新 README"

# 样式修改
git commit -m "style: 调整按钮样式"

# 代码重构
git commit -m "refactor: 重构 API 服务层"

# 性能优化
git commit -m "perf: 优化列表渲染性能"

# 测试
git commit -m "test: 添加单元测试"

# 配置修改
git commit -m "chore: 更新依赖版本"
```

---

## 🔄 分支管理策略

### 单人开发（简单）

```bash
# 直接在 main 分支开发
git pull
# 修改代码...
git add .
git commit -m "feat: 新功能"
git push
```

### 多人开发（推荐）

```bash
# 1. 创建功能分支
git checkout -b feature/user-login

# 2. 开发并提交
git add .
git commit -m "feat: 实现用户登录"
git push origin feature/user-login

# 3. 在 GitHub 创建 Pull Request
# 4. 代码审查后合并到 main
# 5. 合并后自动部署
```

---

## 🛠️ 常用场景

### 场景 1：修改前端界面

```bash
# 1. 修改代码
vim src/views/ProfileView.vue

# 2. 本地预览
npm run dev

# 3. 满意后提交
git add .
git commit -m "style: 优化配置文件列表界面"
git push

# 4. 等待自动部署
```

### 场景 2：修改后端 API

```bash
# 1. 修改代码
vim server/routes/profiles.js

# 2. 本地测试
npm run server

# 3. 提交
git add .
git commit -m "feat: 添加批量删除接口"
git push
```

### 场景 3：紧急修复

```bash
# 1. 立即修复
vim src/App.vue

# 2. 快速提交
git add .
git commit -m "fix: 修复页面崩溃问题"
git push

# 3. 监控部署
# GitHub Actions → 查看进度
```

### 场景 4：回滚到之前版本

```bash
# 1. 查看历史
git log --oneline

# 2. 回滚（不删除历史）
git revert <commit-hash>

# 3. 推送
git push

# 4. 自动部署旧版本
```

---

## 🔍 检查部署状态

### 在 GitHub 查看

```
1. 打开 GitHub 仓库
2. 点击 Actions 标签
3. 看到工作流列表
4. 点击最新的工作流查看详情
```

### 工作流步骤

```
✅ Checkout code        - 拉取代码
✅ Setup Node.js        - 配置环境
✅ Install dependencies - 安装依赖
✅ Build project        - 构建项目
✅ Deploy to server     - 部署到服务器
✅ Deployment status    - 显示结果
```

### 在服务器查看

```bash
# SSH 到服务器
ssh root@YOUR_SERVER_IP

# 查看 Docker 容器
docker ps | grep minilpa-web

# 查看日志
docker logs -f minilpa-web

# 查看最新提交
cd /www/wwwroot/minilpa-web
git log --oneline -5
```

---

## ❌ 常见问题

### 问题 1: git push 被拒绝

```bash
# 错误：Updates were rejected...

# 解决：先拉取合并
git pull --rebase
git push
```

### 问题 2: 本地和远程冲突

```bash
# 错误：conflict...

# 解决：
git pull
# 手动解决冲突（编辑冲突文件）
git add .
git commit -m "fix: 解决冲突"
git push
```

### 问题 3: Actions 部署失败

```
1. 查看 Actions 日志
2. 检查错误信息
3. 常见原因：
   - Secrets 配置错误
   - 服务器连接失败
   - 构建失败
   - 磁盘空间不足
```

### 问题 4: 忘记提交消息

```bash
# 如果已经 commit 但消息写错了
git commit --amend -m "正确的消息"

# 如果已经 push 了（谨慎使用）
git commit --amend -m "正确的消息"
git push --force
```

---

## 🚀 高效工作流

### 使用 VS Code

1. **安装扩展**
   - GitLens
   - Git Graph
   - GitHub Pull Requests

2. **VS Code 内置 Git**
   - 左侧源代码管理图标
   - 可视化查看修改
   - 点击提交和推送

### 使用 Git 命令别名

```bash
# 添加到 ~/.gitconfig 或 ~/.zshrc

alias gs='git status'
alias ga='git add .'
alias gc='git commit -m'
alias gp='git push'
alias gl='git pull'
alias glog='git log --oneline --graph'

# 使用
gs              # 查看状态
ga              # 添加所有
gc "消息"       # 提交
gp              # 推送
```

---

## 📊 开发环境设置

### 本地开发配置

```bash
# 1. 安装依赖
npm install

# 2. 创建环境变量文件（可选）
cat > .env.local << EOF
VITE_API_BASE_URL=http://localhost:3001
EOF

# 3. 启动开发服务器
npm run dev

# 4. 启动后端服务（另一个终端）
npm run server
```

### 推荐的开发工具

- **编辑器**: VS Code
- **终端**: Windows Terminal / iTerm2
- **Git GUI**: GitHub Desktop / SourceTree
- **API 测试**: Postman / Thunder Client

---

## ✅ 完整流程检查清单

**一次性设置：**
- [ ] 从 GitHub 克隆代码到本地
- [ ] 安装 Node.js 和依赖
- [ ] 配置 Git SSH 或 Token
- [ ] 测试本地开发环境

**日常开发：**
- [ ] git pull 拉取最新代码
- [ ] 修改代码
- [ ] 本地测试
- [ ] git add + commit + push
- [ ] 查看 GitHub Actions
- [ ] 验证部署结果

---

## 🎯 快速开始

```bash
# 在本地电脑执行

# 1. 克隆仓库
git clone https://github.com/YOUR_USERNAME/minilpa-web.git
cd minilpa-web

# 2. 安装依赖
npm install

# 3. 测试运行
npm run dev

# 4. 开发功能
# ... 修改代码 ...

# 5. 提交部署
git add .
git commit -m "feat: 我的第一次提交"
git push

# 6. 查看部署
# 打开 GitHub → Actions
```

---

**现在您可以在本地开发，推送到 GitHub 后自动部署！** ✨

需要帮助设置本地环境吗？告诉我您的操作系统（Windows/Mac/Linux）！

